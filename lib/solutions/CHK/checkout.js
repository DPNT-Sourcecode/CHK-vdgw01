'use strict';

//noinspection JSUnusedLocalSymbols
module.exports = function (skus) {
    if (skus === "A") {
        return 50
    } else if (skus === "B") {
       return 30 
    } else if (skus === "C") {
        return 20
    }
};